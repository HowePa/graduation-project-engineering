# 模板

Markdown 文件保留 v0.1 文档结构；evidence-manifest.json 仅数据示意，不能复制其散列与时间作为证据。

v0.2 新增 default-thesis.yaml：默认论文格式与章节模板，可由 Resolver 自动采用。它不是学校 DOCX 模板，也不代表已实现 Word 生成。用户 DOCX/PDF 格式附件须核实后提取规则。ui-presets.yaml 提供四套风格，默认推荐 A/C/D，必须由用户选择。
